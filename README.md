# Imeginary-File-Hierarchy


I created imeginary file hierarchy ( similar one to Linux )

There are some basic operations:
    -ls   : list files and directories
    -cd   : change directory
    -cp   : copy file
